events {}
http {
        upstream backend {
                server ip:8080 weight=1;
                server ip:8080 weight=2;
		        server ip:8080 weight=3;
        }
 
        server {
                listen 80;
                location / {
                        proxy_pass http://backend;
		}
	}
}