NOTE: Go through the following steps only after ininitially starting your tinc vpn server.

VPN client setup:
# Generate the keypairs with command, tincd -n netname -K4096
# Copy /etc/tinc/netname/hosts/experimental to vpn server at /etc/tinc/netname/hosts/experimental destination

# Now go through the 'kubernetes' section steps provided in 'roles/k8s_master_tinc/templates/steps.txt'